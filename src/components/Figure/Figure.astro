---
interface Props {
  src: string;
  alt?: string;
}

const { src, alt } = Astro.props;
---

{alt ? (
  <figure>
    <img src={src} alt={alt} />
    <figcaption>{alt}</figcaption>
  </figure>
) : (
  <img src={src} alt="" />
)}
